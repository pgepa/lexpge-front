export type UserProfileToken = {
    nome: string;
    email: string;
    id_perfil: string;
    token: string;
}

export type UserProfile = {
    email: string;
    nome: string;
    id_perfil: string;
}