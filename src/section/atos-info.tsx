import { SectionTitle } from '@/section/section-title'
import { UserRound } from 'lucide-react'

export const AtoNormativoInfoSection = () => {
    return (
        <div>
            <SectionTitle title="Informações Básicas" icon={UserRound} />
        </div>
    )
}